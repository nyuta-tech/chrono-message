version: '3'
services:
  client:
    build:
      context: ./client
    volumes:
      - ./client:/src
    ports:
      - 3000:3000
    command: sh -c "cd app && npm install && npm start"
    tty: true